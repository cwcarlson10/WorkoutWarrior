<%= simple_form_for(@athlete, remote: true) do |f| %>
  <%= f.input :name %>
  <% if @old_athlete.present? %>
    <%= f.button :submit, "Update Account", remote: true %>
  <% else %>
    <%= f.button :submit, "Create Account" %>
  <% end %>
<% end %>
