<%= simple_form_for(@athlete) do |f| %>
  <%= f.input :name %>
  <% if @old_athlete.present? %>
    <%= f.button :submit, "Update Account" %>
  <% else %>
    <%= f.button :submit, "Create Account" %>
  <% end %>
<% end %>
